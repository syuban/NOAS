import * as styled from "../../styles/common/header/header";

const Header = () => {
    return (
        <styled.Wrapper>
            Header
        </styled.Wrapper>
    );
};

export default Header;
